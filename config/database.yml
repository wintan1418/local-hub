# PostgreSQL. Versions 9.3 and up are supported.
#
# Install the pg driver:
#   gem install pg
# On macOS with Homebrew:
#   gem install pg -- --with-pg-config=/usr/local/bin/pg_config
# On Windows:
#   gem install pg
#       Choose the win32 build.
#       Install PostgreSQL and put its /bin directory on your path.
#
# Configure Using Gemfile
# gem "pg"
#
default: &default
  adapter: postgresql
  encoding: unicode
  # For details on connection pooling, see Rails configuration guide
  # https://guides.rubyonrails.org/configuring.html#database-pooling
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  url: <%= ENV['DATABASE_URL'] || 'postgresql://postgres:password@localhost:5432/localservicehub_development' %>

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  <<: *default
  url: <%= ENV['TEST_DATABASE_URL'] || 'postgresql://postgres:password@localhost:5432/localservicehub_test' %>

# Production database configuration
production:
  <<: *default
  # Force use of DATABASE_URL in production
  url: <%= ENV['DATABASE_URL'] %>
  # Add connection timeout and retry settings for Render
  connect_timeout: 10
  checkout_timeout: 5
  reaping_frequency: 10

  # SolidCable database configuration
  cable:
    <<: *default
    url: <%= ENV['DATABASE_URL'] %>