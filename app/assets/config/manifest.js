// Application JavaScript
//= link application.js

// Note: CSS is served directly from app/assets/builds via public path, not processed by Sprockets

// Note: JavaScript files are handled by Import Maps, not the asset pipeline
// Propshaft automatically includes all files in app/assets/images and app/assets/builds
