<div class="bg-indigo-50 rounded-xl shadow p-5 flex flex-col gap-2">
  <div class="flex items-center gap-2 mb-1">
    <svg class="w-5 h-5 text-indigo-400" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"/></svg>
    <span class="font-semibold text-indigo-700"><%= booking.service.title %></span>
  </div>
  <div class="text-gray-700 text-sm mb-1">
    <span class="font-medium">Date:</span> <%= booking.scheduled_at.strftime('%A, %b %d, %Y at %I:%M %p') %>
  </div>
  <div class="text-gray-700 text-sm mb-1">
    <span class="font-medium">Status:</span> <span class="capitalize"><%= booking.status %></span>
  </div>
  <div class="flex items-center gap-2 mt-2">
    <%= link_to 'View Service', service_path(booking.service), class: "px-3 py-1 bg-indigo-600 text-white rounded hover:bg-indigo-700 text-xs" %>
  </div>
</div> 